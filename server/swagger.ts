import swaggerJSDoc from 'swagger-jsdoc';

export const swaggerOptions: swaggerJSDoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'TSL E-Archive API',
      version: '1.0.0',
      description: 'API documentation for TSL E-Archive server'
    },
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT'
        }
      }
    },
    security: [
      {
        bearerAuth: []
      }
    ]
  },
  apis: ['./route/*.ts', './controllers/*.ts']
};

export const swaggerSpec = swaggerJSDoc(swaggerOptions);
